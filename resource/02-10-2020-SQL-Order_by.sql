USE employees;

SELECT *
FROM employees
ORDER BY emp_no DESC;


SELECT *
FROM employees
ORDER BY first_name; # order by first name

SELECT *
FROM employees
ORDER BY first_name, last_name; # order by first name than last name

SELECT *
FROM employees
ORDER BY first_name, last_name DESC; # order by first name, than last name but last name in descending order



